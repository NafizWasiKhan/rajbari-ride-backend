[build]
builder = "nixpacks"
buildCommand = "cd backend && pip install -r ../requirements.txt"

[deploy]
startCommand = "cd backend && python manage.py migrate && gunicorn rajbari_ride.wsgi --bind 0.0.0.0:$PORT"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
